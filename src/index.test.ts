describe("📨 API Tests", () => {
    describe("Directories", () => {
        it.todo("should get an random question from root")
        it.todo("should get an random question from a directory")
        it.todo("should fail when getting an random question from an invalid directory")
        it.todo("should fail when getting get an random question from a question")
    })
    
    describe("Questions", () => {
        it.todo("should return an specific question")
        it.todo("should fail when returning an invalid question")
    })
})